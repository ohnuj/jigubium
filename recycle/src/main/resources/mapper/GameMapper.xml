<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="first_project.recycle.mapper.GameMapper">

    <!--[1. 오늘 플레이 횟수 조회 쿼리]-->
    <select id="countTodayGamesByMemberId" resultType="int">
        SELECT COUNT(*)
        FROM game_history
        WHERE member_id = #{memberId}
          AND played_at >= CURRENT_DATE
          AND played_at &lt; CURRENT_DATE + INTERVAL 1 DAY
    </select>


     <!--[2. 게임 이력 저장 쿼리]-->

    <insert id="insertGameHistory" useGeneratedKeys="true" keyProperty="gameHistoryId">
        INSERT INTO game_history (
            member_id,
            score,
            total_count,
            correct_count,
            played_at
        ) VALUES (
                     #{memberId},
                     #{score},
                     #{totalCount},
                     #{correctCount},
                     NOW()
                 )
    </insert>
</mapper>