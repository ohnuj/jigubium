<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- UserMapper 인터페이스와 매핑 -->
<mapper namespace="first_project.recycle.mapper.UserMapper">

    <!-- [일반 회원가입] provider='LOCAL', role='USER', point=100은 DB 기본값으로 자동 저장 -->
    <insert id="insert" parameterType="first_project.recycle.domain.User" useGeneratedKeys="true" keyProperty="memberId">
        INSERT INTO member (email, password, name, nickname)
        VALUES (#{email}, #{password}, #{name}, #{nickname})
    </insert>

    <!-- [일반 로그인] 이메일 일치 회원 조회 (비밀번호 검증 및 세션 정보/보유 포인트 확인용) -->
    <select id="findByEmail" resultType="first_project.recycle.domain.User">
        SELECT member_id, password, nickname, role, point
        FROM member
        WHERE email = #{email}
    </select>

    <!-- [이메일 중복 체크] 가입 시 동일한 이메일 존재 여부 확인 (0: 사용 가능, 1 이상: 중복) -->
    <select id="countByEmail" resultType="int">
        SELECT COUNT(*)
        FROM member
        WHERE email = #{email}
    </select>

    <!-- [소셜 로그인] 소셜 식별값(provider, providerId)으로 기존 가입 여부 및 회원 전체 정보 조회 -->
    <select id="findByProviderAndProviderId" resultType="first_project.recycle.domain.User">
        SELECT member_id,
               nickname,
               email,
               provider,
               provider_id,
               name,
               role,
               point
        FROM member
        WHERE provider = #{provider}
          AND provider_id = #{providerId}
    </select>

    <!-- [소셜 신규 가입] 비밀번호 없이 소셜 연동 정보 저장 (role='USER', point=100은 DB 기본값 자동 적용) -->
    <insert id="insertOAuthUser" parameterType="first_project.recycle.domain.User" useGeneratedKeys="true" keyProperty="memberId">
        INSERT INTO member (email, name, nickname, provider, provider_id)
        VALUES (#{email}, #{name}, #{nickname}, #{provider}, #{providerId})
    </insert>

</mapper>