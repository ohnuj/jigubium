<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="first_project.recycle.repository.BoardImageMapper">

<!--    사진 등록-->
    <insert id="insertBoardImage" parameterType="first_project.recycle.domain.BoardImage">

    INSERT INTO board_image (board_id,
                             image_url,
                             sort_order
    )
    VALUES (
            #{boardId},
            #{imageUrl},
            #{sortOrder}
           )
    </insert>
</mapper>